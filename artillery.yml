config:
  target: http://localhost:3000
  phases:
    - duration: 3
      arrivalRate: 1
  engines:
    playwright:
      launchOptions:
        headless: true
  processor: './src/tests/load/artillery/datavisualization.ts'
scenarios:
  - engine: playwright
    testFunction: 'artilleryScript'
