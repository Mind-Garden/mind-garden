config:
  target: http://localhost:3000
  phases:
    - duration: 60
      arrivalRate: 4
  engines:
    playwright: 
      launchOptions:
        headless: true
  processor: './src/tests/load/artillery/login.ts'
scenarios:
  - engine: playwright
    testFunction: 'artilleryScript'
