config:
  target: http://localhost:3000
  phases:
    - duration: 1
      arrivalRate: 1
  engines:
    playwright: 
      launchOptions:
        headless: false
  processor: './src/tests/load/artillery/artillery-test.ts'
scenarios:
  - engine: playwright
    testFunction: 'artilleryScript'
